# CompanyVerse backend
# Python 3.10+
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0

# Generative AI — required when LLM_PROVIDER=azure_openai or foundry.
# The default (LLM_PROVIDER=local) offline engine needs none of these.
openai>=1.40,<2.0
# Only needed for the keyless Microsoft Entra path (AZURE_OPENAI_USE_AAD=true).
azure-identity>=1.25,<2.0
# Foundry IQ knowledge base retrieval — required when PROVIDER=foundry.
# Pinned to the beta that exposes KnowledgeBaseRetrievalClient.
azure-search-documents==12.1.0b1
# aiohttp (used by azure-search-documents async client) on Windows/Python 3.14
# doesn't trust system root CAs; certifi provides a trusted bundle.
certifi>=2024.0
# azure-ai-projects is installed alongside for agent/project operations.
azure-ai-projects>=2.2,<3.0
